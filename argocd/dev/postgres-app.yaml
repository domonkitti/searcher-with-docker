apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: searchable-postgres-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_ORG/YOUR_REPO.git
    targetRevision: HEAD
    path: k8s/overlays/dev/postgres
  destination:
    server: https://kubernetes.default.svc
    namespace: searchable-from-book-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
